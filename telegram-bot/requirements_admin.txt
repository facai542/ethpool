# Admin Bot Dependencies
# 解决依赖冲突：python-telegram-bot 20.7 需要 httpx~=0.25.2
# supabase 使用较旧版本以兼容 httpx 0.25.2
# Pillow 使用更新的版本以支持 Python 3.14
# 注意：ckzg 需要 C 编译器，如果在 Windows 上安装失败，可以跳过（可选依赖）
python-telegram-bot==20.7
supabase==2.8.1
httpx==0.25.2
httpcore==1.0.9
tronpy==0.4.0
web3==6.11.3
qrcode[pil]==7.4.2
Pillow>=10.3.0
python-dotenv==1.0.0
aiohttp==3.9.1
# ckzg 是可选依赖，如果构建失败可以跳过
# 如果需要，请安装 Visual Studio Build Tools

