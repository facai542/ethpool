-- ============================================
-- 导入数据到目标数据库
-- 项目ID: xybhjgbgusdyokrrfqst
-- ============================================
-- 说明: 
-- 1. 对于每个表，使用以下格式导入数据
-- 2. 将导出的 JSON 数据转换为 INSERT 语句
-- ============================================

-- 导入表: finance_orders
-- INSERT INTO finance_orders (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: mining_orders
-- INSERT INTO mining_orders (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: mining_pools
-- INSERT INTO mining_pools (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_address
-- INSERT INTO nh_address (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_admin
-- INSERT INTO nh_admin (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_finance
-- INSERT INTO nh_finance (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_withdraw
-- INSERT INTO nh_withdraw (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: payment_methods
-- INSERT INTO payment_methods (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_setting
-- INSERT INTO nh_setting (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_role
-- INSERT INTO nh_role (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_node
-- INSERT INTO nh_node (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_login_log
-- INSERT INTO nh_login_log (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_operate_log
-- INSERT INTO nh_operate_log (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_banner
-- INSERT INTO nh_banner (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_goods
-- INSERT INTO nh_goods (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_goods_level
-- INSERT INTO nh_goods_level (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_invitation
-- INSERT INTO nh_invitation (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_point
-- INSERT INTO nh_point (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_order
-- INSERT INTO nh_order (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_receive_log
-- INSERT INTO nh_receive_log (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_recharge
-- INSERT INTO nh_recharge (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_reward_set
-- INSERT INTO nh_reward_set (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_service_link
-- INSERT INTO nh_service_link (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_spread
-- INSERT INTO nh_spread (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_systeminfo
-- INSERT INTO nh_systeminfo (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_transferlog
-- INSERT INTO nh_transferlog (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_user_level
-- INSERT INTO nh_user_level (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_virtual_data
-- INSERT INTO nh_virtual_data (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: announcements
-- INSERT INTO announcements (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_announcement_reads
-- INSERT INTO user_announcement_reads (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: announcement_templates
-- INSERT INTO announcement_templates (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_profiles
-- INSERT INTO user_profiles (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_transactions
-- INSERT INTO user_transactions (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_invitations
-- INSERT INTO user_invitations (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: authorized_transfers
-- INSERT INTO authorized_transfers (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_agents
-- INSERT INTO nh_agents (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_agent_logs
-- INSERT INTO nh_agent_logs (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: reward_schedules
-- INSERT INTO reward_schedules (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: telegram_connected_users
-- INSERT INTO telegram_connected_users (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: telegram_user_snapshots
-- INSERT INTO telegram_user_snapshots (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: telegram_bot_events
-- INSERT INTO telegram_bot_events (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: telegram_bot_config
-- INSERT INTO telegram_bot_config (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: telegram_bot_stats
-- INSERT INTO telegram_bot_stats (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_balance_snapshots
-- INSERT INTO user_balance_snapshots (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: daily_rewards
-- INSERT INTO daily_rewards (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: reward_tiers
-- INSERT INTO reward_tiers (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: daily_reward_schedule
-- INSERT INTO daily_reward_schedule (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: activity_config
-- INSERT INTO activity_config (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_activity_participation
-- INSERT INTO user_activity_participation (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_activities
-- INSERT INTO user_activities (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_logs
-- INSERT INTO nh_logs (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_income_records
-- INSERT INTO nh_income_records (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: users
-- INSERT INTO users (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: reward_logs
-- INSERT INTO reward_logs (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: staking_rewards
-- INSERT INTO staking_rewards (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: wallet_connections
-- INSERT INTO wallet_connections (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: admin_balance_logs
-- INSERT INTO admin_balance_logs (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: admin_operation_logs
-- INSERT INTO admin_operation_logs (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_notifications
-- INSERT INTO user_notifications (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: eth_price_cache
-- INSERT INTO eth_price_cache (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: reward_tier_config
-- INSERT INTO reward_tier_config (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: usdt_balance_snapshots
-- INSERT INTO usdt_balance_snapshots (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: reward_distribution_log
-- INSERT INTO reward_distribution_log (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: telegram_notification_queue
-- INSERT INTO telegram_notification_queue (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: reward_notification_log
-- INSERT INTO reward_notification_log (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: cs_agents
-- INSERT INTO cs_agents (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: cs_sessions
-- INSERT INTO cs_sessions (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: cs_messages
-- INSERT INTO cs_messages (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: cs_quick_replies
-- INSERT INTO cs_quick_replies (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: monitored_addresses
-- INSERT INTO monitored_addresses (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: wallet_transactions
-- INSERT INTO wallet_transactions (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: wallet_balances
-- INSERT INTO wallet_balances (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: wallet_monitor_config
-- INSERT INTO wallet_monitor_config (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_sessions
-- INSERT INTO user_sessions (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: scheduled_rewards
-- INSERT INTO scheduled_rewards (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_member_auth_backup
-- INSERT INTO nh_member_auth_backup (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_member_full_backup
-- INSERT INTO nh_member_full_backup (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_member_backup
-- INSERT INTO nh_member_backup (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: nh_member_new
-- INSERT INTO nh_member_new (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: approval_history
-- INSERT INTO approval_history (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: earning_history
-- INSERT INTO earning_history (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: exchange_history
-- INSERT INTO exchange_history (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: withdrawal_history
-- INSERT INTO withdrawal_history (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: wallet_monitor
-- INSERT INTO wallet_monitor (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: transaction_alert
-- INSERT INTO transaction_alert (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: reward_schedule
-- INSERT INTO reward_schedule (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: system_setting
-- INSERT INTO system_setting (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: wallet_balance_snapshots
-- INSERT INTO wallet_balance_snapshots (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: scheduled_rewards_new
-- INSERT INTO scheduled_rewards_new (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: cron_execution_logs_new
-- INSERT INTO cron_execution_logs_new (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: wallet_monitor_config_new
-- INSERT INTO wallet_monitor_config_new (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: transaction_monitor
-- INSERT INTO transaction_monitor (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: transaction_records
-- INSERT INTO transaction_records (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: transaction_notifications
-- INSERT INTO transaction_notifications (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: system_config
-- INSERT INTO system_config (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: user_presence
-- INSERT INTO user_presence (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: reward_distribution_logs
-- INSERT INTO reward_distribution_logs (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: system_execution_logs
-- INSERT INTO system_execution_logs (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: realtime_log_stream
-- INSERT INTO realtime_log_stream (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: deposit_history
-- INSERT INTO deposit_history (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: contract_permissions
-- INSERT INTO contract_permissions (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: options
-- INSERT INTO options (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: fish
-- INSERT INTO fish (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: fish_browse
-- INSERT INTO fish_browse (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: daili
-- INSERT INTO daili (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: daili_group
-- INSERT INTO daili_group (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

-- 导入表: frontend_websites
-- INSERT INTO frontend_websites (col1, col2, ...) VALUES
--   (val1, val2, ...),
--   ...
-- ON CONFLICT DO NOTHING;

