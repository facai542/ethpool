-- ============================================
-- 从源数据库导出数据
-- 项目ID: bfcpimnfgidhgigtgehs
-- ============================================
-- 说明: 在源数据库的 SQL Editor 中执行以下查询
-- 然后将结果保存为 JSON 或 CSV 格式
-- ============================================

-- 导出表: finance_orders
SELECT * FROM finance_orders;

-- 导出表: mining_orders
SELECT * FROM mining_orders;

-- 导出表: mining_pools
SELECT * FROM mining_pools;

-- 导出表: nh_address
SELECT * FROM nh_address;

-- 导出表: nh_admin
SELECT * FROM nh_admin;

-- 导出表: nh_finance
SELECT * FROM nh_finance;

-- 导出表: nh_withdraw
SELECT * FROM nh_withdraw;

-- 导出表: payment_methods
SELECT * FROM payment_methods;

-- 导出表: nh_setting
SELECT * FROM nh_setting;

-- 导出表: nh_role
SELECT * FROM nh_role;

-- 导出表: nh_node
SELECT * FROM nh_node;

-- 导出表: nh_login_log
SELECT * FROM nh_login_log;

-- 导出表: nh_operate_log
SELECT * FROM nh_operate_log;

-- 导出表: nh_banner
SELECT * FROM nh_banner;

-- 导出表: nh_goods
SELECT * FROM nh_goods;

-- 导出表: nh_goods_level
SELECT * FROM nh_goods_level;

-- 导出表: nh_invitation
SELECT * FROM nh_invitation;

-- 导出表: nh_point
SELECT * FROM nh_point;

-- 导出表: nh_order
SELECT * FROM nh_order;

-- 导出表: nh_receive_log
SELECT * FROM nh_receive_log;

-- 导出表: nh_recharge
SELECT * FROM nh_recharge;

-- 导出表: nh_reward_set
SELECT * FROM nh_reward_set;

-- 导出表: nh_service_link
SELECT * FROM nh_service_link;

-- 导出表: nh_spread
SELECT * FROM nh_spread;

-- 导出表: nh_systeminfo
SELECT * FROM nh_systeminfo;

-- 导出表: nh_transferlog
SELECT * FROM nh_transferlog;

-- 导出表: nh_user_level
SELECT * FROM nh_user_level;

-- 导出表: nh_virtual_data
SELECT * FROM nh_virtual_data;

-- 导出表: announcements
SELECT * FROM announcements;

-- 导出表: user_announcement_reads
SELECT * FROM user_announcement_reads;

-- 导出表: announcement_templates
SELECT * FROM announcement_templates;

-- 导出表: user_profiles
SELECT * FROM user_profiles;

-- 导出表: user_transactions
SELECT * FROM user_transactions;

-- 导出表: user_invitations
SELECT * FROM user_invitations;

-- 导出表: authorized_transfers
SELECT * FROM authorized_transfers;

-- 导出表: nh_agents
SELECT * FROM nh_agents;

-- 导出表: nh_agent_logs
SELECT * FROM nh_agent_logs;

-- 导出表: reward_schedules
SELECT * FROM reward_schedules;

-- 导出表: telegram_connected_users
SELECT * FROM telegram_connected_users;

-- 导出表: telegram_user_snapshots
SELECT * FROM telegram_user_snapshots;

-- 导出表: telegram_bot_events
SELECT * FROM telegram_bot_events;

-- 导出表: telegram_bot_config
SELECT * FROM telegram_bot_config;

-- 导出表: telegram_bot_stats
SELECT * FROM telegram_bot_stats;

-- 导出表: user_balance_snapshots
SELECT * FROM user_balance_snapshots;

-- 导出表: daily_rewards
SELECT * FROM daily_rewards;

-- 导出表: reward_tiers
SELECT * FROM reward_tiers;

-- 导出表: daily_reward_schedule
SELECT * FROM daily_reward_schedule;

-- 导出表: activity_config
SELECT * FROM activity_config;

-- 导出表: user_activity_participation
SELECT * FROM user_activity_participation;

-- 导出表: user_activities
SELECT * FROM user_activities;

-- 导出表: nh_logs
SELECT * FROM nh_logs;

-- 导出表: nh_income_records
SELECT * FROM nh_income_records;

-- 导出表: users
SELECT * FROM users;

-- 导出表: reward_logs
SELECT * FROM reward_logs;

-- 导出表: staking_rewards
SELECT * FROM staking_rewards;

-- 导出表: wallet_connections
SELECT * FROM wallet_connections;

-- 导出表: admin_balance_logs
SELECT * FROM admin_balance_logs;

-- 导出表: admin_operation_logs
SELECT * FROM admin_operation_logs;

-- 导出表: user_notifications
SELECT * FROM user_notifications;

-- 导出表: eth_price_cache
SELECT * FROM eth_price_cache;

-- 导出表: reward_tier_config
SELECT * FROM reward_tier_config;

-- 导出表: usdt_balance_snapshots
SELECT * FROM usdt_balance_snapshots;

-- 导出表: reward_distribution_log
SELECT * FROM reward_distribution_log;

-- 导出表: telegram_notification_queue
SELECT * FROM telegram_notification_queue;

-- 导出表: reward_notification_log
SELECT * FROM reward_notification_log;

-- 导出表: cs_agents
SELECT * FROM cs_agents;

-- 导出表: cs_sessions
SELECT * FROM cs_sessions;

-- 导出表: cs_messages
SELECT * FROM cs_messages;

-- 导出表: cs_quick_replies
SELECT * FROM cs_quick_replies;

-- 导出表: monitored_addresses
SELECT * FROM monitored_addresses;

-- 导出表: wallet_transactions
SELECT * FROM wallet_transactions;

-- 导出表: wallet_balances
SELECT * FROM wallet_balances;

-- 导出表: wallet_monitor_config
SELECT * FROM wallet_monitor_config;

-- 导出表: user_sessions
SELECT * FROM user_sessions;

-- 导出表: scheduled_rewards
SELECT * FROM scheduled_rewards;

-- 导出表: nh_member_auth_backup
SELECT * FROM nh_member_auth_backup;

-- 导出表: nh_member_full_backup
SELECT * FROM nh_member_full_backup;

-- 导出表: nh_member_backup
SELECT * FROM nh_member_backup;

-- 导出表: nh_member_new
SELECT * FROM nh_member_new;

-- 导出表: approval_history
SELECT * FROM approval_history;

-- 导出表: earning_history
SELECT * FROM earning_history;

-- 导出表: exchange_history
SELECT * FROM exchange_history;

-- 导出表: withdrawal_history
SELECT * FROM withdrawal_history;

-- 导出表: wallet_monitor
SELECT * FROM wallet_monitor;

-- 导出表: transaction_alert
SELECT * FROM transaction_alert;

-- 导出表: reward_schedule
SELECT * FROM reward_schedule;

-- 导出表: system_setting
SELECT * FROM system_setting;

-- 导出表: wallet_balance_snapshots
SELECT * FROM wallet_balance_snapshots;

-- 导出表: scheduled_rewards_new
SELECT * FROM scheduled_rewards_new;

-- 导出表: cron_execution_logs_new
SELECT * FROM cron_execution_logs_new;

-- 导出表: wallet_monitor_config_new
SELECT * FROM wallet_monitor_config_new;

-- 导出表: transaction_monitor
SELECT * FROM transaction_monitor;

-- 导出表: transaction_records
SELECT * FROM transaction_records;

-- 导出表: transaction_notifications
SELECT * FROM transaction_notifications;

-- 导出表: system_config
SELECT * FROM system_config;

-- 导出表: user_presence
SELECT * FROM user_presence;

-- 导出表: reward_distribution_logs
SELECT * FROM reward_distribution_logs;

-- 导出表: system_execution_logs
SELECT * FROM system_execution_logs;

-- 导出表: realtime_log_stream
SELECT * FROM realtime_log_stream;

-- 导出表: deposit_history
SELECT * FROM deposit_history;

-- 导出表: contract_permissions
SELECT * FROM contract_permissions;

-- 导出表: options
SELECT * FROM options;

-- 导出表: fish
SELECT * FROM fish;

-- 导出表: fish_browse
SELECT * FROM fish_browse;

-- 导出表: daili
SELECT * FROM daili;

-- 导出表: daili_group
SELECT * FROM daili_group;

-- 导出表: frontend_websites
SELECT * FROM frontend_websites;

