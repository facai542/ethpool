# Railway 配置文件
# 确保 Railway 只构建 approval-monitor 目录

build:
  builder: NIXPACKS
  nixpacksPath: nixpacks.toml
  watchPatterns:
    - "**/*.js"
    - "**/*.json"
    - "!node_modules/**"

deploy:
  numReplicas: 1
  restartPolicyType: ON_FAILURE
  restartPolicyMaxRetries: 10

