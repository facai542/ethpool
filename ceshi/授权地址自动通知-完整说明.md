# ✅ 授权地址自动实时USDT动账通知 - 完整说明

## 🎯 回答你的问题

**是的！配置完 Tokenview 后，通过 Supabase Realtime 就可以实现：**
1. ✅ 授权地址自动添加到监控
2. ✅ 机器人实时USDT动账通知

## 🔄 完整自动化流程

```
┌──────────────────────────────────────────────────────────┐
│  第1步：用户授权地址                                       │
│  - 用户在前端连接钱包                                      │
│  - 授权 USDT 合约                                         │
└──────────────────┬───────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────┐
│  第2步：后端处理授权                                       │
│  - POST /api/user/authorize                               │
│  - 保存到 nh_member_new 表                                │
│  - 保存到 wallet_monitor 表                               │
└──────────────────┬───────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────┐
│  第3步：自动添加到监控服务 ✨ NEW                          │
│  - 自动添加到 Moralis Stream (主要)                        │
│  - 自动添加到 Tokenview (备用)                             │
│  - 双重保障，提高可靠性                                    │
└──────────────────┬───────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────┐
│  第4步：监听链上交易                                       │
│  - Moralis/Tokenview 实时监听以太坊主网                    │
│  - 检测 USDT 合约 Transfer 事件                            │
│  - 过滤只监控授权的地址                                    │
└──────────────────┬───────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────┐
│  第5步：Webhook 接收交易                                   │
│  - Moralis → /api/moralis/webhook                         │
│  - Tokenview → /api/tokenview/webhook                     │
│  - 解析交易数据（from, to, amount, hash）                  │
└──────────────────┬───────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────┐
│  第6步：保存到数据库                                       │
│  - 写入 wallet_transactions 表                             │
│  - 自动去重（相同 tx_hash 只保存一次）                      │
└──────────────────┬───────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────┐
│  第7步：触发通知机制                                       │
│  方式1：数据库触发器 (推荐)                                 │
│    - 自动写入 telegram_notification_queue                  │
│                                                            │
│  方式2：Supabase Realtime                                  │
│    - 监听 INSERT 事件                                       │
│    - 写入通知队列                                           │
└──────────────────┬───────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────┐
│  第8步：Telegram 实时服务                                  │
│  - TelegramRealtimeService 监听通知队列                    │
│  - Supabase Realtime 实时推送                              │
│  - 获取用户信息和余额                                       │
│  - 格式化通知消息                                           │
└──────────────────┬───────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────┐
│  第9步：发送 Telegram 通知 🎉                              │
│  - 发送到 Telegram 群组                                    │
│  - 显示完整交易信息                                         │
│  - 标记为已发送                                             │
└──────────────────────────────────────────────────────────┘
```

## ✅ 已配置完成的部分

### 1. Webhook 接收端点 ✅
- ✅ `/api/moralis/webhook` - Moralis 推送
- ✅ `/api/tokenview/webhook` - Tokenview 推送（新增）

### 2. 自动添加到监控 ✅
- ✅ `addAddressToMoralisStream()` - Moralis 自动添加
- ✅ `addAddressToTokenview()` - Tokenview 自动添加（新增）
- ✅ `addAddressToMonitoring()` - 统一调用（新增）

### 3. 数据库表结构 ✅
- ✅ `wallet_monitor` - 监控地址列表
- ✅ `wallet_transactions` - 交易记录
- ✅ `telegram_notification_queue` - 通知队列

### 4. Telegram 实时服务 ✅
- ✅ `TelegramRealtimeService` - Supabase Realtime 监听
- ✅ 自动格式化通知消息
- ✅ 发送到 Telegram 群组

## ⚙️ 需要的环境变量

### Vercel 环境变量（必需）

```bash
# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=你的Supabase URL
SUPABASE_SERVICE_ROLE_KEY=你的Service Role Key

# Moralis 配置（主要监控）
MORALIS_API_KEY=你的Moralis API Key

# Telegram 配置
TELEGRAM_BOT_TOKEN=你的Bot Token
TELEGRAM_CHAT_ID=你的群组ID

# 应用配置
NEXT_PUBLIC_APP_URL=https://ethmax.vercel.app
```

### Vercel 环境变量（可选 - Tokenview）

```bash
# Tokenview 配置（备用监控）
TOKENVIEW_API_KEY=你的Tokenview API Key
TOKENVIEW_WEBHOOK_SECRET=fSpkcrSkEMrMOxY65gr0
```

## 🚀 部署步骤

### 1. 提交代码到 Git

```bash
git add .
git commit -m "添加 Tokenview 自动监控支持"
git push
```

### 2. Vercel 自动部署

Vercel 会自动检测到 push 并开始部署（约1-2分钟）

### 3. 配置环境变量

在 Vercel Dashboard 添加上述环境变量

### 4. 重新部署

添加环境变量后，点击 "Redeploy" 重新部署

## 🧪 测试流程

### 测试1：授权并添加监控

1. 连接钱包并授权
2. 查看 Vercel 日志：
   ```
   🔍 添加地址到监控服务... 0x...
   📡 添加地址到 Moralis Stream: 0x...
   📡 添加地址到 Tokenview 监控: 0x...
   ✅ 地址已添加到监控服务（Moralis/Tokenview）
   ```

### 测试2：发送测试交易

1. 向授权地址发送小额 USDT（如 0.1 USDT）
2. 等待区块确认（约 15 秒）
3. 查看 Vercel 日志：
   ```
   📡 收到 Moralis Webhook
   📝 保存交易到数据库: 0x... in 0.100000 USDT
   ✅ 交易已保存，触发器将自动发送通知
   ```

### 测试3：检查 Telegram 通知

在 Telegram 群组应该收到：
```
🟢收入USDT 提醒       +0.100000 USDT

钱包余额：100.000000
顶层代理：默认代理
代理昵称：直链注册
用户编号：123
用户备注：测试用户
是否活动：是
用户钱包：0x...
订单金额: +0.100000 USDT
授权时间:2025-10-11 14:30:00
交易对象:0x...
执行操作：客户转入
```

## 🔍 数据库触发器（推荐方案）

如果使用数据库触发器，需要在 Supabase SQL Editor 执行：

```sql
-- 创建触发器函数
CREATE OR REPLACE FUNCTION trigger_transaction_notification()
RETURNS TRIGGER AS $$
BEGIN
  -- 当新交易插入时，自动创建通知
  IF NEW.is_processed = false THEN
    INSERT INTO telegram_notification_queue (
      notification_type,
      user_address,
      notification_data,
      is_sent,
      created_at
    ) VALUES (
      'usdt_transaction',
      NEW.user_address,
      jsonb_build_object(
        'transactionType', CASE WHEN NEW.transaction_type = 'in' THEN 'income' ELSE 'expense' END,
        'transactionAmount', NEW.amount::numeric,
        'txHash', NEW.tx_hash,
        'fromAddress', NEW.from_address,
        'toAddress', NEW.to_address,
        'token', NEW.token,
        'timestamp', NEW.timestamp
      ),
      false,
      NOW()
    );
  END IF;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- 创建触发器
CREATE TRIGGER trg_transaction_notification
  AFTER INSERT ON wallet_transactions
  FOR EACH ROW
  EXECUTE FUNCTION trigger_transaction_notification();
```

## 📊 监控方案对比

### 方案1：仅 Moralis（推荐新手）

**优点：**
- ✅ 配置简单
- ✅ 免费额度充足
- ✅ 实时性好

**缺点：**
- ❌ 单点故障
- ❌ 免费额度有限

**配置：**
只需要 `MORALIS_API_KEY`

### 方案2：Moralis + Tokenview（推荐生产）

**优点：**
- ✅ 双重保障
- ✅ 自动故障转移
- ✅ 自动去重
- ✅ 更高可靠性

**缺点：**
- ❌ 配置略复杂
- ❌ Tokenview 需付费

**配置：**
需要 `MORALIS_API_KEY` + `TOKENVIEW_API_KEY`

## 🎯 最终答案

**是的！通过以下配置，可以实现授权地址自动添加机器人实时USDT动账通知：**

1. ✅ **自动监控**  
   - 用户授权后，地址自动添加到 Moralis/Tokenview
   - 无需手动操作

2. ✅ **实时推送**  
   - Moralis/Tokenview 实时监听链上交易
   - Webhook 立即推送到你的服务器

3. ✅ **Supabase Realtime**  
   - TelegramRealtimeService 监听通知队列
   - 实时获取新通知并发送

4. ✅ **Telegram 通知**  
   - 自动格式化消息
   - 发送到指定群组
   - 包含完整交易信息

5. ✅ **自动去重**  
   - 同一交易只通知一次
   - Moralis 和 Tokenview 同时推送也不会重复

## 🚨 注意事项

1. **环境变量**  
   必须在 Vercel 配置所有必需的环境变量

2. **数据库触发器**  
   推荐使用触发器自动创建通知（更可靠）

3. **Telegram Bot**  
   确保 Bot 已加入群组并有发送消息权限

4. **测试小额**  
   首次测试建议发送小额USDT（如 0.1 USDT）

5. **查看日志**  
   出现问题时，查看 Vercel 日志进行排查

## 📞 获取帮助

- Vercel 日志：https://vercel.com/dashboard → Logs
- Supabase Dashboard：https://supabase.com/dashboard
- Moralis Dashboard：https://admin.moralis.io
- Tokenview Dashboard：https://services.tokenview.io/en/dashboard

---

**创建时间：** 2025-10-11  
**状态：** ✅ 配置完成，可以使用

